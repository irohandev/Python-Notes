# first install the command in terminal = pip isntall virtualenv
# for creating virtual Environment = python -m venv venv_name
# for activating virtual Environment = 1) .\venv_name\Scripts\activate {for Windows}
#                                      2) source/env_name/bin/activate {for Mac & Linux}
# when you are inside the virtual Environment just install the necessary module which will be required and install using PIP INSTALL command....after installation and working is complete uninstall using PIP UNINSTALL command 
# for deactivating virtual Environment just type = deactivate